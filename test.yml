---
 - hosts: dev
   tasks:
    - name: Ensure SELinux is set to enforcing mode
      lineinfile:
       path: /opt/test
       regexp: '^-->'
       line: --> SAMEENA=Sameena
       line: --> gdgdgdgd
